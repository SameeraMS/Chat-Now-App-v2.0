create database chatapp;

use chatapp;

CREATE TABLE user(
                     username VARCHAR(20) PRIMARY KEY,
                     password VARCHAR(20),
                     image LONGBLOB
);

CREATE TABLE chat(
                     username VARCHAR(20),
                     id VARCHAR(20),
                     massage VARCHAR(250),
                     image LONGBLOB,
                     time VARCHAR(30)
);